# local secrets -- never commit
env.local.sh
.env
.env.*
*.key

# openevolve run outputs
openevolve_output/
results/
checkpoints/
*.log

# rendered videos / eval artifacts (heavy, regenerable)
renders/
*.mp4
vbench_cache/
